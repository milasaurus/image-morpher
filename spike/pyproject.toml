[project]
name = "image-morpher-spike"
version = "0.0.1"
description = "Single-file spike validating the round-0 → strategy → round-1 loop."
requires-python = ">=3.11"
dependencies = [
    "luma-agents",
    "anthropic>=0.40",
    "python-dotenv>=1.0",
]
